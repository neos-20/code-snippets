"Sieve of Eratosthenes ": {
    "prefix": "sieveofe",
    "body": [
      "/* C++ program to print all primes smaller than or equal to ",
      "n using Sieve of Eratosthenes with Time complexity : O(n*log(log(n))) */",
      "#include <bits/stdc++.h>",
      "using namespace std;",
      "",
      "void SieveOfEratosthenes(int n)",
      "{",
      "    bool primeNo[n + 1];",
      "    memset(primeNo, true, sizeof(primeNo));",
      "",
      "    for (int p = 2; p * p <= n; p++)",
      "    {",
      "",
      "        if (primeNo[p] == true)",
      "        {",
      "",
      "            for (int i = p * p; i <= n; i += p)",
      "                primeNo[i] = false;",
      "        }",
      "    }",
      "",
      "    // Print all prime numbers",
      "    for (int p = 2; p <= n; p++)",
      "        if (primeNo[p])",
      "            cout << p << \" \";",
      "}",
      ""
    ],
    "description": "Sieve of Eratosthenes "
  }