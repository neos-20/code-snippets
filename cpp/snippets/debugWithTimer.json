"debug with time elapsed": {
  "prefix": "debugwithtimer",
  "body": [
    "#include<bits/stdc++.h>",
    "#define fasterthanlight ios_base::sync_with_stdio(false); cin.tie(NULL); cout.tie(NULL)",
    "#define int long long",
    "#define endl \"\\n\"",
    "#define MAX 1e9",
    "#define MIN -1e9",
    "#define mod 1000000007",
    "#define pb push_back",
    "#define all(x) (x).begin(), (x).end()",
    "#define debug(x) cerr<<#x<<'='<<(x)<<endl;",
    "#define watch(x) cerr << \"\\n\" << (#x) << \" is \" << (x) << endl;",
    "#define output(x) cout << ( x ? \"YES\" : \"NO\" ) << '\\n' ;",
    "#define mxn 100005",
    "using namespace std;",
    "",
    "void solve()",
    "{",
    "	int n;",
    "    n = 2*2;",
    "    watch(n); //output : n is 4",
    "    int exponent = 2;",
    "    watch(pow(n,exponent)); //output: pow(n,exponent) is 16",
    "}",
    "",
    "signed main()",
    "{",
    "	fasterthanlight;",
    "	int T; T = 1;",
    "	// cin >> T;",
    "	while (T--)",
    "	{",
    "        debug(T);",
    "		solve();",
    "	}",
    "	cout << \"\\n\\n\"<<\"Time elapsed: \" << 1.0 * clock() / CLOCKS_PER_SEC << \" seconds.\\n\";",
    "	return 0;",
    "}"
  ],
  "description": "debug with time elapsed"
}
